{% extends "pages/admin/base.html.twig" %}
{% block body %}
	<div class="d-flex justify-content-between align-items-center">
		<h2>Gestion des Catégories</h2>
		<button class="btn btn-success">
			<i class="bi bi-plus-circle"></i>
			Ajouter categorie
		</button>
	</div>
	<div class="row my-4">
		<div class="col-12 col-xl-12 mb-4 mb-lg-0">
			<div class="card">
				<div class="card-body">
					<div class="table-responsive">
						<table id="table-categories" data-toggle="table" data-pagination="true" data-page-size="10" data-search="true" data-locale="fr-FR" class="table">
							<thead>
								<tr>
									<th data-field="id">Id</th>
									<th data-field="name">Nom</th>
									<th data-field="slug">Slug</th>
									<th data-formatter="actionFormatter" data-events="actionEvents">Action</th>
								</tr>
							</thead>
							<tbody>
								{% for category in categories %}
									<tr>
										<td>{{ category.id }}</td>
										<td>{{ category.name }}</td>
										<td>{{ category.slug }}</td>
										<td></td>
									</tr>
								{% endfor %}
							</tbody>
						</table>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
