<nav class="navbar navbar-light bg-light p-3">
	<div class="d-flex col-12 col-md-3 col-lg-2 mb-2 mb-lg-0 flex-wrap flex-md-nowrap justify-content-between">
		<a class="navbar-brand" href="{{ path('home') }}">
			MyBlog Dashboard
		</a>
		<button class="navbar-toggler d-md-none collapsed mb-3" type="button" data-bs-toggle="collapse" data-bs-target="#sidebar" aria-controls="sidebar" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
	</div>
	<div class="col-12 col-md-5 col-lg-8 d-flex align-items-center justify-content-md-end mt-3 mt-md-0 mobile-menu-links">
		<div class="dropdown d-none d-md-block">
			<button class="btn dropdown-toggle" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-expanded="false">
				{% if user.avatar starts with 'http://' or user.avatar starts with 'https://' %}
					<img src="{{ user.avatar }}" class="img-rounded" alt="{{ user.username }}" width="30" height="30">
				{% else %}
					<img src="{{ asset('../uploads/avatars/' ~ user.avatar ~ '.jpg') }}" class="img-rounded" alt="{{ user.username }}" width="30" height="30">
				{% endif %}
				<span>{{ user.username }}
				</span>
			</button>
			<ul class="dropdown-menu" aria-labelledby="dropdownMenuButton">
				<li>
					<a class="dropdown-item" href="{{ path('blog') }}">Retour sur le blog</a>
				</li>
				<li>
					<a class="dropdown-item" href="{{ path('home') }}">Retour sur le site</a>
				</li>
				<li>
					<a class="dropdown-item" href="{{ path('my_profile') }}">Mon profil</a>
				</li>
				<li>
					<a class="dropdown-item" href="{{ path('logout') }}">Déconnexion</a>
				</li>
			</ul>
		</div>
	</div>
</nav>
